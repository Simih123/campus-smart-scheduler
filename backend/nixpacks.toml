[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install uvicorn fastapi sqlalchemy pydantic icalendar python-multipart"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
